---
const PROJECTS = [
  {
    title: "PETS",
    description: "Pagina creada para agendar citas de mascotas",
    link: "https://calm-parfait-f66837.netlify.app/",
    github: "#",
    image: "/projects/svgl.webp",
    tags: ["HTML", "CSS", "JavaScript", "React"],
  },
];
---

{
  PROJECTS.map(({ image, title, description, tags }) => (
    <article>
      <h3 class="text-2xl font-semibold text-yellow-200 mb-20">{title}</h3>
      <p class="text-lg mb-4 text-pretty">{description}</p>
      <ul>
        {tags.map((tag) => (
          <li>{tag}</li>
        ))}
      </ul>
      <img
        class="rounded  shadow-2xl shadow-white/10"
        src={image}
        alt={`Captura de pantalla del proyecto ${image}`}
      />
    </article>
  ))
}
